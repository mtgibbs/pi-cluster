apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-library-nfs
  annotations:
    # Prevent Flux from deleting this PV
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  nfs:
    server: storage.lab.mtgibbs.dev
    path: /share/cluster/photos
  mountOptions:
    - hard
    - rsize=1048576
    - wsize=1048576
