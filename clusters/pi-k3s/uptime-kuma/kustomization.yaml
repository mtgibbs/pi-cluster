apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: uptime-kuma
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # Monitor setup requires manual configuration via web UI
  # The uptime-kuma-api library has compatibility issues with v2
  # See monitors-configmap.yaml for suggested monitors to add
