T1: in scripts/ralph-status.sh make the status sweep configurable — replace the hardcoded `-mmin +1440` at the find in hb_init with `-mmin "+${RALPH_STATUS_KEEP_MIN:-1440}"`, matching how ralph-log.sh:44 already honours RALPH_LOG_KEEP_MIN. Default behaviour must not change. This is FIRST because until it lands, pointing RALPH_STATUS_DIR at committed files deletes them daily. Do not touch the log sweep, the JSON shape, or hb_mark. Document the new variable in the header contract block alongside RALPH_STATUS_DIR.
T2: in scripts/ralph-log.sh and scripts/ralph-status.sh default the roots to the TARGET REPO's .evidence/ instead of $HOME — LOG_ROOT becomes "$(git -C "${ROOT:-.}" rev-parse --show-toplevel)/.evidence/runs" and HB_DIR becomes ".../.evidence/status", each still overridable by RALPH_LOG_DIR / RALPH_STATUS_DIR used verbatim exactly as today. Fall back to the current $HOME path ONLY when the target is not a git repository. Create the directories if absent. Do not migrate anything that already exists under ~/.harness.
T3: in scripts/ralph-qwen.sh and scripts/ralph-codex.sh run scripts/loop-index.py against the target repo after each task completes, before the loop moves on, so the record is distilled while the raw logs still exist. Best-effort: a failure to index must warn and never fail the task, same contract as ralph-log.sh. Pass the repo root explicitly; do not assume the cwd.
T4: split "did work happen" from "was evidence collected" — they are two questions and the loop currently answers both by asking `git status`. (a) In scripts/ralph-qwen.sh the no-op guard at the `changed nothing` check must scope to the work: `git -C "$ROOT" status --porcelain -- . ':!.evidence'`. Today a task with nothing left to do still passes the guard because the harness's own status heartbeat landed in .evidence/status/, so the loop committed a "T1" whose entire content was one status JSON. (b) In scripts/ralph-judge.sh the preflight at line 70 must use the same pathspec; the judge refused to run at all because loop-index.py had just written .evidence/index.md, which T3 makes happen after every task by design. (c) Excluding .evidence/ from the WORK question creates a blind spot, so ADD the positive one: after a task's commit, assert that .evidence/index.jsonl exists and contains a row whose "task" matches the task label just committed, and warn loudly to stderr if not — loop-index.py is best-effort and a silent indexing failure must not look like a healthy run. Do not change what the loop does otherwise: no task selection, retry contract or gate semantics.
