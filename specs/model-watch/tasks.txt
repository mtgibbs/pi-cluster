T1: Write clusters/pi-k3s/model-watch/model-watch.py — sweep the HuggingFace API for recently-trending text-generation models, compute the intake gates (params, MoE config, licence, Q4 fit) from real API metadata, assign each candidate a bucket, and print the §10 DRY_RUN output contract. Stdlib only.
T2: Add the LLM summary + ntfy push to model-watch.py — classified list to LiteLLM for prose only, then publish, including the fallback digest that still pushes when the LLM call fails.
T3: Write the four manifests in clusters/pi-k3s/model-watch/ — namespace, external-secret (litellm-api-key + reused ntfy/family-password), cronjob (monthly, mounts the script), kustomization with configMapGenerator.
T4: Wire it up — add the `ntfy access family "model-watch" rw` line to clusters/pi-k3s/ntfy/deployment.yaml and the Kustomization entry to clusters/pi-k3s/flux-system/infrastructure.yaml.
