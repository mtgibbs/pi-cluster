T1: Implement the whole model-watch feature — the script, all four manifests, the ntfy ACL line, and the Flux registration — until `bash specs/model-watch/verify.sh` prints VERIFY: PASS. verify.sh is a single end-to-end gate covering every file, so partial work cannot pass it; do all of it in one pass, then run the gate and fix what it reports.
