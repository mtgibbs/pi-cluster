T1: create clusters/pi-k3s/matrix/namespace.yaml (namespace "matrix") and kustomization.yaml listing all eight manifests in spec section 9 dependency order (namespace, external-secret, pvc, postgresql, synapse-config, synapse, element, ingress)
T2: create clusters/pi-k3s/matrix/external-secret.yaml — mirror clusters/pi-k3s/mealie/external-secret.yaml exactly (onepassword ClusterSecretStore, engineVersion v2 template); render a POSTGRES_PASSWORD scalar plus a homeserver-secrets.yaml key per spec sections 3 and 6, pulling 1Password item "matrix" fields db-password, registration-shared-secret, macaroon-secret, form-secret
T3: create clusters/pi-k3s/matrix/pvc.yaml — two local-path PVCs with annotation kustomize.toolkit.fluxcd.io/prune disabled: matrix-postgresql-data (5Gi) and synapse-data (5Gi), per spec section 3
T4: create clusters/pi-k3s/matrix/postgresql.yaml — mirror clusters/pi-k3s/mealie/postgresql.yaml renamed to matrix-postgresql, POSTGRES_DB synapse, POSTGRES_USER synapse, password from matrix-secret; pin to pi5-worker-2; keep the postgres:16-alpine image and resource limits
T5: create clusters/pi-k3s/matrix/synapse-config.yaml ConfigMap — paste the spec section 6 homeserver.yaml base VERBATIM plus a standard Synapse log.config; no secrets here
T6: create clusters/pi-k3s/matrix/synapse.yaml — Deployment (initContainer generate-keys + main synapse container, both matrixdotorg/synapse:v1.156.0 running the two merged --config-path files) plus a ClusterIP Service on 8008, per spec section 6; nodeSelector pi5-worker-2; requests 512Mi/250m limits 1Gi/1000m; readiness/liveness httpGet /health on 8008
T7: create clusters/pi-k3s/matrix/element.yaml — ConfigMap config.json (spec section 6), Deployment using vectorim/element-web:v1.12.23, ClusterIP Service on 80; nodeSelector pi5-worker-2; requests 16Mi limits 64Mi
T8: create clusters/pi-k3s/matrix/ingress.yaml — mirror clusters/pi-k3s/mealie/ingress.yaml; two hosts, matrix.lab.mtgibbs.dev routing to service synapse port 8008 and element.lab.mtgibbs.dev routing to service element port 80; letsencrypt-prod, nginx, proxy-body-size 0; NEVER reference chat.lab
T9: register a Flux Kustomization named matrix in clusters/pi-k3s/flux-system/infrastructure.yaml, mirroring the existing mealie Kustomization block exactly (path ./clusters/pi-k3s/matrix, dependsOn external-secrets-config and ingress and cert-manager-config, sourceRef flux-system, same interval and prune settings as neighbors)
