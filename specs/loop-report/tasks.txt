T1: create scripts/loop-report.sh per spec.md §3 — arg parsing (--spec required else `usage:` to stderr exit 1; --base default origin/main; --judge-state default "$(git rev-parse --git-path ralph-judge)"; --gate-log optional), then print EXACTLY: line1 `== loop report ==`, line2 `branch: <git branch --show-current>`, line3 `base: <ref>  commits: <git rev-list --count base..HEAD>` (two spaces before commits:), line4 `gate: <payload>` where payload = the single line immediately after the LAST line that is exactly `---GATE-SCORE---` in the --gate-log file, verbatim, or `none` if no --gate-log or no sentinel. Line5 is T2's (`judge: none` placeholder is fine for T1). bash with set -uo pipefail, macOS bash 3.2 compatible, no declare -A, executable bit set.
T2: extend scripts/loop-report.sh — line5 `judge: <payload>` per spec.md §3: if "<judge-state>/report.json" exists and `jq empty` accepts it, print `judge: accepted=<n> rejected=<n> gate-gaps=<n> outcome=<o>` using jq lengths of .accepted/.rejected/.gate_gaps and .outcome; any missing file or parse failure → `judge: none`. Do not change lines 1-4. Fixtures at specs/loop-report/fixtures/ show the shape: report.json with accepted:["a","b"], rejected:["c"], gate_gaps:[], outcome:"dry".
