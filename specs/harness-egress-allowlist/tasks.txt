T1: in the beelink-ansible repo create the directory files/harness-egress/ containing allowlist.conf and the proxy's own config, using the proxy chosen in OQ2 of specs/harness-egress-allowlist/spec.md — the proxy MUST start in log-only mode, meaning it permits every destination and writes one line per connection naming the host and port, and allowlist.conf MUST exist but MAY be empty apart from comments because phase 1 is observation and the real entries come from T4 after a week of traffic; every non-comment line you do add must have a "# why" comment on the line directly above it and must not use a wildcard broader than one label, both of which the gate checks. DO NOT write any nftables or iptables rule in this task, DO NOT set any proxy environment variable on any container, and DO NOT flip the proxy to enforcing — those are T2, T3 and phase 2 respectively, and doing them here fails the gate.
T2: in the beelink-ansible compose template that defines the coding-harness services, add the harness-egress service and put the four harness containers plus harness-egress on a user-defined network that declares an explicit fixed subnet under its ipam config, because container IPs are not stable and the phase-2 firewall rule has to match on the subnet; then set HTTP_PROXY, HTTPS_PROXY and NO_PROXY on each of coding-harness-qwen, coding-harness-claude, coding-harness-claude-2 and coding-harness-codex so they point at the harness-egress service, and NO_PROXY MUST contain litellm so that model traffic to http://litellm:4000 is never routed through the proxy — that single omission is the most likely way to break every loop on the box, per section 6 of the spec. DO NOT add NET_ADMIN, DO NOT set privileged true, and DO NOT mount /var/run/docker.sock on any service including harness-egress, all three of which are safeguards the gate fails on; DO NOT remove or weaken the existing cap_drop ALL, no-new-privileges, read-only rootfs or non-root user settings on the harness containers.
T3: in beelink-ansible/playbooks/50-ai-stack.yml wire the harness-egress service and the files/harness-egress/ config directory into the existing deployment flow, following the same shape the playbook already uses for the other beelink-* sidecar containers so the whole thing is reproducible from a clean checkout with no manual step; the copy or template task for files/harness-egress/ must be idempotent so a re-run does not duplicate anything. DO NOT add a firewall task, DO NOT change the enforcement mode, and DO NOT edit .claude/skills/coding-agent-ops/SKILL.md in the pi-cluster repo — the SKILL.md update is T7 and belongs to phase 2 after enforcement actually ships.
