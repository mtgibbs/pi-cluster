T1: in the beelink-ansible repo create the directory files/harness-egress/ containing allowlist.conf and the proxy's own config, using the proxy chosen in OQ2 of specs/harness-egress-allowlist/spec.md — the proxy MUST start in log-only mode, meaning it permits every destination and writes one line per connection naming the host and port, and allowlist.conf MUST exist but MAY be empty apart from comments because phase 1 is observation and the real entries come from T4 after a week of traffic; every non-comment line you do add must have a "# why" comment on the line directly above it and must not use a wildcard broader than one label, both of which the gate checks. DO NOT write any firewall rule in this task, DO NOT set any proxy environment variable on any container, DO NOT touch the networks, and DO NOT flip the proxy to enforcing — those belong to T2 and phase 2, and doing them here fails the gate.
T2: in playbooks/50-ai-stack.yml declare a NEW network named harness-net with an explicit fixed subnet under its ipam config, then MOVE the four services coding-harness-qwen, coding-harness-claude, coding-harness-claude-2 and coding-harness-codex off ai-internal and onto harness-net, put harness-egress on harness-net, and add harness-net to the litellm service's networks list so litellm sits on BOTH ai-internal and harness-net — litellm keeping a foot on harness-net is what preserves http://litellm:4000 by name, and omitting it breaks every loop on the box. Leave all fourteen other services on ai-internal exactly as they are, because ai-internal is what keeps them OUT of the phase-2 blast radius. Then set HTTP_PROXY, HTTPS_PROXY and NO_PROXY on each of the four harness containers pointing at harness-egress, with NO_PROXY containing litellm. DO NOT add NET_ADMIN, DO NOT set privileged true, DO NOT mount /var/run/docker.sock on any service including harness-egress, and DO NOT weaken the existing cap_drop ALL, no-new-privileges, read_only or non-root settings — the gate fails on all of these. DO NOT write any firewall rule; that is phase 2.
T3: in playbooks/50-ai-stack.yml wire the files/harness-egress/ config directory into the existing deployment flow with a copy or template task that is idempotent on re-run, following the same shape the playbook already uses for the other beelink-* sidecar config under files/. DO NOT add a ufw or DOCKER-USER task, DO NOT change the proxy enforcement mode, and DO NOT edit .claude/skills/coding-agent-ops/SKILL.md in the pi-cluster repo — the SKILL.md update is T7 and belongs to phase 2 after enforcement actually ships.
