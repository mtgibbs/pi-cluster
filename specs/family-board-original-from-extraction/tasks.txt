T1: Implement original_from extraction per spec.md §9 — surgical edits across five files: (a) clusters/pi-k3s/n8n/workflows/inbound-mail.json — append `ALTER TABLE intake_items ADD COLUMN IF NOT EXISTS original_from TEXT;` to the Ensure Table query before the CREATE UNIQUE INDEX; in the Build Request node's jsCode, add `,"originalFrom":string|null` to the JSON schema description in the system prompt plus one sentence telling the model to populate it from forwarded-header blocks; in the Parse Records node's jsCode, map `original_from: r.originalFrom || null,` next to the other snake_case fields; in the Store node's SQL, append `, original_from` to the column list, `, $16` to VALUES, `, original_from = EXCLUDED.original_from` to the ON CONFLICT DO UPDATE SET clause, and `,={{ $json.original_from }}` to the queryReplacement; keep $1..$15 byte-identical and don't change node graph or connections. (b) clusters/pi-k3s/n8n/workflows/src/build-request.js — mirror the same prompt addition. (c) clusters/pi-k3s/n8n/workflows/src/parse-records.js — mirror the same `original_from: r.originalFrom || null,` mapping; the item_key formula stays unchanged. (d) clusters/pi-k3s/n8n/workflows/feed-api.json — in the Get Feed query, insert `i.original_from,` between `i.source_from,` and `COALESCE(a.acks, '[]'::json) AS acks`. (e) clusters/pi-k3s/family-board/index.html — in the detailsHTML function, push a new `<div class="d-row"><span class="d-lab">Originally from</span><span class="d-val">'+esc(it.original_from)+'</span></div>` row only when `it.original_from` is truthy, between the Email subject row and the Confidence row. Both JSON files must remain valid; no other files change.
