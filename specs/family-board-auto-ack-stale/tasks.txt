T1: In clusters/pi-k3s/n8n/workflows/feed-api.json, modify ONLY the `Get Feed` node's `parameters.query` string. Replace exactly `COALESCE(a.acks, '[]'::json) AS acks` with `CASE WHEN i.received_at < now() - interval '7 days' THEN '["julia","matt","ronin","rory"]'::json ELSE COALESCE(a.acks, '[]'::json) END AS acks`. The surrounding SELECT/FROM/LEFT JOIN/ORDER BY/LIMIT are byte-identical; the query stays a single-line string; the file remains valid JSON; no other node and no other file change.
