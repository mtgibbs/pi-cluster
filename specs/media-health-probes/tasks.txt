T1: add probes to the five servarr manifests clusters/pi-k3s/media/sonarr.yaml clusters/pi-k3s/media/radarr.yaml clusters/pi-k3s/media/lidarr.yaml clusters/pi-k3s/media/readarr.yaml and clusters/pi-k3s/media/prowlarr.yaml — in each file, inside the existing container entry and immediately after its ports: block, add a readinessProbe with httpGet path /ping and port set to that file's containerPort from the section 6 table (sonarr 8989, radarr 7878, lidarr 8686, readarr 8787, prowlarr 9696) plus initialDelaySeconds 10 periodSeconds 10 timeoutSeconds 5 failureThreshold 3, and a livenessProbe with the same httpGet path and port plus initialDelaySeconds 120 periodSeconds 30 timeoutSeconds 10 failureThreshold 6; match the surrounding indentation exactly and change nothing else in any of the five files
T2: add probes to clusters/pi-k3s/media/sabnzbd.yaml — same two probe blocks and same timings as T1, but httpGet path /api?mode=version and port 8080; change nothing else in the file
T3: add tcpSocket probes to clusters/pi-k3s/media/bazarr.yaml clusters/pi-k3s/media/calibre-web.yaml clusters/pi-k3s/media/qbittorrent.yaml and clusters/pi-k3s/media/lazylibrarian.yaml — in each, inside the existing container entry and immediately after its ports: block, add a readinessProbe with tcpSocket port set to that file's containerPort (bazarr 6767, calibre-web 8083, qbittorrent 8080, lazylibrarian 5299) plus initialDelaySeconds 10 periodSeconds 10 timeoutSeconds 5 failureThreshold 3, and a livenessProbe with the same tcpSocket port plus initialDelaySeconds 120 periodSeconds 30 timeoutSeconds 10 failureThreshold 6; DO NOT use httpGet for any of these four — section 6 explains that bazarr answers 200 on every path and would give a permanently green probe; change nothing else
T4: add tcpSocket probes to clusters/pi-k3s/media/jellyseerr.yaml clusters/pi-k3s/media/flaresolverr.yaml clusters/pi-k3s/pihole/pihole-exporter.yaml and clusters/pi-k3s/pihole/pihole-secondary-exporter.yaml — same two probe blocks and same timings as T3, with ports jellyseerr 5055, flaresolverr 8191, pihole-exporter 9617, pihole-secondary-exporter 9617; DO NOT add probes to clusters/pi-k3s/uptime-kuma/autokuma-deployment.yaml or clusters/pi-k3s/private-exit-node/deployment.yaml, which declare no containerPort and are out of scope; change nothing else
