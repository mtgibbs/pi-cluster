T1: In clusters/pi-k3s/family-board/index.html, inside the existing empty `<aside class="drawer" aria-hidden="true">…</aside>` (from L1), add ONE `<div class="drawer-section">` containing: a header "Connect", a subhead "Scan to subscribe on a phone", then exactly TWO `<div class="qr-card">` blocks (Ronin first, then Rory). Each qr-card has: an `<h3>` label (e.g. "Ronin's calendar"), an `<img>` with the matching src (`/qr-cal-ronin.svg` for Ronin, `/qr-cal-rory.svg` for Rory) sized ~180×180px on white, a short instruction line ("Scan with your phone's camera — tap Subscribe"), and the literal webcal URL beneath as readable text (Ronin: `webcal://n8n-hook.mtgibbs.dev/webhook/cal-ronin-dd5bef4b-4b18-4965-96af-c0b26117ee4a`; Rory: `webcal://n8n-hook.mtgibbs.dev/webhook/cal-rory-d4ba8436-1a91-46e8-ad42-d3a13c85f56e`). Add CSS rules for `.drawer-section` (padding) and `.qr-card` (rounded var(--r), padding, margin between cards). Do NOT touch the `<aside class="drawer">` tag itself or its `aria-hidden` attribute. Do NOT add any JavaScript — open/close behavior is already shipped (L2/L3). Don't touch any other file. All L1/L2/L3 anchors (.shell, drawerOpen, .shell.open, DRAWER_WIDTH, pointerdown, the keydown handler, the // ack toggle listener, quadHTML) remain byte-identical.
