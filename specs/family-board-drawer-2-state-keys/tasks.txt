T1: In clusters/pi-k3s/family-board/index.html, add `let drawerOpen = false;` near the other module-level state vars (viewer, acks, menu); add CSS `.shell { transition: transform .28s var(--pop); }` and `.shell.open { transform: translateX(-360px); }`; EXTEND the existing `document.addEventListener("keydown", e => {…})` listener (do NOT replace it, keep the `A`-key art-mode branch, the `poke()` fall-through, and the `e.target.matches("input, textarea")` typing guard) — add a Backquote branch that toggles drawerOpen + the `.open` class + `aria-hidden` on `.drawer` when NOT typing, and an Escape branch that closes the drawer when `drawerOpen`; add `document.querySelector(".wrap").addEventListener("click", …)` that closes the drawer when `drawerOpen` is true (attach to `.wrap`, NOT to `main` — the existing ack toggle listens on `main` and must not be touched). No pointer events, no EDGE_ZONE/COMMIT_THRESHOLD/DRAWER_WIDTH constants, no .sliver.grabbing CSS in this sub-spec (L3 adds those).
