T1: in scripts/ralph-log.sh and scripts/ralph-status.sh, file run artefacts under the spec slug. Add a `_ralph_slug <spec-dir>` helper to BOTH files, duplicated verbatim (each must stay independently sourceable, so neither may depend on the other — say so in a comment in each): basename of the spec dir, lowercased, non-[a-z0-9._-] collapsed to '-', runs of '-' squeezed, leading '-.' and trailing '-' stripped, truncated to 40 chars, empty result becomes "nospec". In ralph-log.sh set LOG_SLUG="${HB_SLUG:-$(_ralph_slug "${SPEC_DIR:-}")}" (prefer the heartbeat's slug so the two stores cannot disagree) and LOG_DIR="$LOG_ROOT/$LOG_SLUG/${HB_AGENT:-${RALPH_AGENT:-agent}}-$$". In ralph-status.sh set HB_SLUG="$(_ralph_slug "$HB_SPEC")" and descend into it — but keep the FIRST `HB_DIR=` assignment carrying both `${RALPH_STATUS_DIR:` and `.evidence/status` on one line, because specs/evidence-convention AC-1's gate greps exactly that line; assign the root to HB_DIR, copy it to HB_STATUS_ROOT, then append the slug. The leaf name must remain EXACTLY `<agent>-<pid>` — do not fold the slug into it (loop-index.py parses the pid with split("-", 1)[1]). Update both header contract blocks. Do not touch the sweeps yet.

T2: in scripts/ralph-log.sh and scripts/ralph-status.sh, make expiry per-run rather than per-spec. In ralph-log.sh the reap must become `-mindepth 2 -maxdepth 2` — depth 1 is now the SPEC, and a directory's mtime tracks its newest child, so reaping there deletes a whole feature's history the moment it goes quiet — then a second pass `-mindepth 1 -maxdepth 1 -type d -empty -delete` to remove slug dirs the reap emptied. In ralph-status.sh root the existing `-name "${HB_AGENT}-*.json"` sweep at HB_STATUS_ROOT, not at HB_DIR, so it still spans every spec rather than only the one being run. ORDER MATTERS AND DIFFERS BETWEEN THE TWO FILES: ralph-log.sh mkdirs LOG_DIR (depth 2) before sweeping, so its slug dir is never empty and never a candidate; ralph-status.sh does not write its file until the first hb_write, so it must sweep FIRST and mkdir HB_DIR after, or the -empty pass deletes the directory out from under it. Keep RALPH_LOG_KEEP_MIN and RALPH_STATUS_KEEP_MIN and their current defaults exactly as they are — the 1440 status default is out of scope and is pinned by another spec's gate.

T3: in scripts/loop-index.py, make run enumeration executor-agnostic and stop fabricating names. Replace the three hardcoded "qwen-*" leaf globs with a module-level RUN_GLOBS = ("qwen-*", "codex-*") plus a glob_runs(d) helper; every codex run is currently invisible to store 3 — the dirs are written and nothing ever lists them. Give harness_roots a leaf_globs parameter defaulting to RUN_GLOBS (still tolerating a single string) and derive its run-dir skip-list from RUN_GLOBS rather than repeating the prefixes. In load_log_dirs capture both halves of the leaf (`agent, pid = name.split("-", 1)`) and store "agent", "run_dir" and the discovered scope on the record. In load_status carry through the "agent" and "spec" fields that ralph-status.sh already writes and that were being dropped. Add a run_name(r) helper returning r["run_dir"] or a name composed from that record's OWN agent, and use it at the three render sites that currently build f"qwen-{pid}" — which names a path that does not exist for any codex run. Add "agent", "run_dir" and "spec" to the per-task runs projection, which is a whitelist and silently drops anything not listed.
