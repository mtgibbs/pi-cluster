T1: In clusters/pi-k3s/family-board/index.html, add module-level constants `const DRAWER_WIDTH = 360;`, `const EDGE_ZONE = 32;`, `const COMMIT_THRESHOLD = 0.3;` near other constants (FEED_URL etc); add `pointerdown`/`pointermove`/`pointerup` (and `pointercancel`) listeners on document — pointerdown only starts a drag-open when drawerOpen is false AND `clientX > window.innerWidth - EDGE_ZONE`, OR starts a drag-close when drawerOpen is true (any clientX); pointermove (when dragging) updates `.shell` transform to `translateX(-clamped delta)` and applies a `grabbing` class to either `.sliver` or `.shell` with opacity/box-shadow ramped by `min(1, abs(delta)/DRAWER_WIDTH)`; pointerup commits open/closed based on `COMMIT_THRESHOLD * DRAWER_WIDTH`, clears inline transform, toggles `.open` on `.shell`; add CSS `.shell.dragging { transition: none; }` (suppress transition during drag) and `.sliver.grabbing { opacity: 1; box-shadow: 0 0 24px var(--gold); }` (or `.shell.grabbing .sliver { … }`); add prefers-reduced-motion entry disabling the glow ramp. PRESERVE all L1+L2 anchors: `.shell` wrapper, `.drawer` aside, `.sliver`, `drawerOpen`, `.shell.open` CSS, the keydown handler (A/Backquote/Escape/typing-guard/poke), the `.wrap` click-close, and the existing `// ack toggle` listener — all byte-identical except the additions above.
