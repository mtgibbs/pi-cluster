T1: create clusters/pi-k3s/harness-fleet/ with namespace.yaml declaring the Namespace harness-fleet, external-secret-ghcr.yaml materialising ghcr-pull-secret, and serviceaccount-default.yaml patching the namespace's default ServiceAccount to carry that pull secret. Copy external-secret-ghcr.yaml VERBATIM from clusters/pi-k3s/harness/external-secret-ghcr.yaml changing only the namespace, and cite it in a comment: it reuses the cluster's existing ghcr-read-token, which one 1Password item makes the whole cluster's "pull private packages" identity, so this mints no new credential. The default ServiceAccount is the load-bearing piece and needs a comment saying why: the Job the harness dispatcher renders carries NO imagePullSecrets and NO serviceAccountName, deliberately, because the dispatcher staying describable in a paragraph is a named cliff in that repo's docs/design/fleet-dispatch.md — so a pod with no serviceAccountName runs as default, and a pull secret attached to default applies to every pod in the namespace without any pod naming it. Declaring a ServiceAccount literally named `default` is how that is expressed in GitOps; Kubernetes merges it into the account it auto-creates. Get this wrong and every dispatched run dies at ImagePullBackOff with nothing in the Job to explain why. Declare `namespace: harness-fleet` on each resource rather than a top-level namespace: in kustomization.yaml, matching every file in the sibling harness/ directory. Create kustomization.yaml listing exactly the files this task adds; later tasks extend it. Inline no secret value anywhere.

T2: add the dispatcher's identity and its bounds, in clusters/pi-k3s/harness-fleet/rbac.yaml and resourcequota.yaml, and register both in kustomization.yaml. A ServiceAccount for the dispatcher, a Role — never a ClusterRole, and say why in a comment: a dispatcher that can create Jobs cluster-wide is a dispatcher that can create Jobs in kube-system, and the design doc's phrase is "RBAC scoped to creating Jobs in one namespace" — granting create, get, list, watch and delete on batch/jobs plus get and list on pods and pods/log so a human can read a failed run, and a RoleBinding tying the two together. Grant NO verb on secrets: a component that launches workers does not need to read the credentials those workers receive, and the worker secrets in this namespace include a PAT that can push code. Then a ResourceQuota capping count/jobs.batch at 2, which is the concurrency cap fleet-dispatch.md names, plus requests and limits on cpu and memory so a runaway dispatcher cannot fill the cluster — a Job with backoffLimit 0 does not retry, but nothing stops a dispatcher from creating a hundred of them. State the cap's number in a comment alongside the reason, because a bare "2" reads as arbitrary and the next person to raise it should know what it was protecting.

T3: add the per-strategy worker credentials as ExternalSecrets in clusters/pi-k3s/harness-fleet/, one file per strategy, named harness-worker-build-converge and harness-worker-build-codex, and register them in kustomization.yaml. The harness resolves HARNESS_WORKER_SECRET_<STRATEGY_UPPER_SNAKE> before HARNESS_WORKER_SECRET before nothing, where the suffix is the strategy uppercased with every non-alphanumeric character replaced by an underscore — so build-codex becomes BUILD_CODEX and the secret named harness-worker-build-codex is selected by setting HARNESS_WORKER_SECRET_BUILD_CODEX to it. Per strategy rather than one shared blob because the worker is the least-trusted component in the fleet: it runs a model that writes code into a working tree and then executes that repository's own gate, so a build-codex worker must never hold the LiteLLM key it would never use. Each secret carries that strategy's provider key, HARNESS_REPORT_URL, HARNESS_REPORT_TOKEN, the clone credential, and HARNESS_OUTCOME_PAT. REUSE the existing harness-coordinator/token 1Password item for HARNESS_REPORT_TOKEN — clusters/pi-k3s/harness/external-secret.yaml already materialises it and its header argues the general case — and walk docs/secrets-map.md before minting anything else. No worker secret may carry the dispatch API token: the outcome PAT pushes a branch and opens a PR and may never launch compute, because a worker that can start more workers turns one compromise into a fleet. Inline no secret value; reference 1Password keys only.

T4: write clusters/pi-k3s/harness-fleet/README.md, the runbook for a reader who finds a namespace with nothing running in it. Say plainly that these resources are INERT and why: scripts/dispatch/dispatcher.py and api.py in mtgibbs/harness are not packaged into any image — docker/coordinator.Dockerfile copies coordinator.py and board.html and nothing else, and that repo's CI builds harness-base, loop-executor-opencode and harness-coordinator — and launch() shells out to kubectl, which no image carries. So there is nothing to deploy here yet, and building it is a harness spec rather than this one. Record what unblocks it, as a list a person can act on: an image carrying dispatcher.py, api.py and kubectl; a Deployment using the ServiceAccount T2 creates; the dispatcher env that selects images and secrets per strategy (HARNESS_WORKER_IMAGE_<STRATEGY> and HARNESS_WORKER_SECRET_<STRATEGY>); and the harness-fleet=true node label, which exists nowhere in this repo and whose home would be node-config/. Say which of those are harness's and which are this repo's, because the reason this namespace looks abandoned is that the two halves land in different repositories. Change no manifest in this task.
