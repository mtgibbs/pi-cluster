T1: Implement read-only acks outside your own marker, per spec.md sections 9 and 10 — add the .seat.locked (muted, pointer-events:none) and .seat.mine (highlight ring) CSS rules next to .seat.seen; in quadHTML add viewer-conditional classes (viewer's own seat gets "mine", the other three get "locked" with aria-disabled="true" tabindex="-1"; when viewer is null add neither); and add `if (seat.classList.contains("locked")) return;` as the first guard in the ack click handler. Touch only clusters/pi-k3s/family-board/index.html.
