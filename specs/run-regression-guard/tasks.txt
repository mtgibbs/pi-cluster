T1: edit scripts/ralph-retry.sh — FIRST fix the live bash 3.2 defect, THEN add the run-scoped functions. (a) THE FIX: `retry_regressions` builds a bash array `regressions=()` (declared line 45) and expands it with `for i in "${regressions[@]}"` (line 58). Under `set -u` on macOS bash 3.2 an EMPTY array expansion is a fatal "unbound variable", so the function errors on the HEALTHY path — no regressions. Guard it with `[ ${#regressions[@]} -gt 0 ] && for i in ...` or drop the array for a temp file like the rest of the helper. NOTE: the runtime error reports "line 41", which is the `retry_regressions() {` line — bash 3.2 blames a function's opening line, not the offending expansion. Do not edit line 41. (b) ADD three functions using a SEPARATE state file `${RETRY_STATE_DIR:-${TMPDIR:-/tmp}}/ralph-run-$$.passed`, never the task-scoped `ralph-retry-$$.passed`: `retry_run_init` (set RUN_OK=1 and truncate the run file; on failure RUN_OK=0 and return 0), `retry_run_record <gate-output> [task-label]` (append `<name> <task-label>` for every line classified passed, using the SAME leading-token awk rule already in this file — PASS/OK=passed, FAIL=failed, PEND/PENDING=pending, any other leading token is not a check; task-label defaults to `?`), `retry_run_regressions <gate-output>` (print at most 10 lines `<name> <task-label>` for names present in the run file whose verdict in THIS output is failed OR pending). All five functions return 0 always and print nothing on stderr, in every path including the empty one. macOS bash 3.2: no `declare -A`, no `mapfile`, no unguarded `"${arr[@]}"`.

T2: wire the guard into BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh, identically. (a) Add `retry_run_init` no-op stubs beside the existing `retry_init`/`retry_record`/`retry_regressions` stubs, and call `retry_run_init` ONCE before the task loop (next to the existing `hb_init; log_init` line), never inside it. (b) In the block that begins `if out="$(cd "$ROOT" && bash "$VERIFY" 2>&1)"; then` — AFTER `out` is set and BEFORE `git add -A` — call `retry_run_regressions "$out"`. When it prints nothing, proceed exactly as today (commit, `retry_run_record "$out" "${task%%:*}"` AFTER the commit succeeds, `passed=1`, break). When it prints anything, DO NOT commit: set `feedback` to the literal block below, do not set `passed`, run the same three-step reset the failure path uses (`git reset -q -- .`, `git checkout -- .`, `git clean -fd -- .`), call `log_failure` so the evidence survives, and `continue` to the next attempt. The feedback block, literally: a line `CROSS-TASK REGRESSION — this task broke work an earlier task had already passing:`, then the `<check> (last green at <task>)` lines, then `Restore those without undoing this task's own work. Do not delete or disable an earlier task's code.` Do NOT touch the final STRICT block at the end of either script.

T3: in BOTH scripts, make the exhausted-retries STOP name the destroying task when the cause was a cross-task regression. Track whether the last failure was a regression (e.g. a `_xtask=1` flag set in T2's branch and cleared on any other failure path), and when the retry loop exits without `passed=1` and that flag is set, print to stderr a line containing `CROSS-TASK REGRESSION` and the current task's label instead of the generic `failed verify after N attempts` line, then exit 2 as today. The generic message must still be used for ordinary verify failures. Keep the two scripts identical.
