T1: in harness-console/index.html add the constants TRAVEL_MS=1400 STRAND_FADE_MS=45000 STRAND_MAX=140 ARC_BULGE=0.28 ARC_JITTER=0.10 and a PURE function arcPoint(ax, ay, bx, by, t, bulge) that returns an {x,y} point on a quadratic bezier from (ax,ay) to (bx,by) whose control point is the chord midpoint displaced perpendicular to the chord by bulge multiplied by the chord length — it must read no globals and no canvas state, must return exactly (ax,ay) when t is 0 and exactly (bx,by) when t is 1, and at t=0.5 must sit off the chord; do not change any drawing yet
T2: in harness-console/index.html add a strands array and change emit(fromId, toIds) so that for each resolved target it pushes a strand object {from, to, born, seed} where from and to are the live mind objects, born is performance.now(), and seed is a value derived from the strand count so the jitter is deterministic rather than shimmering per frame; keep the existing sender gust and keep pushing to trans exactly as today so in-flight behaviour is unchanged for now; push NO strand when the message has no target
T3: in harness-console/index.html add stepStrands(now) that drops strands whose age exceeds STRAND_FADE_MS and, while strands.length exceeds STRAND_MAX, removes from the front so the oldest go first; call it from frame() alongside stepTrans
T4: in harness-console/index.html add drawStrands(now) that renders every live strand as an arc built from arcPoint using the strand's from.x/from.y and to.x/to.y read live each frame so the rope stays attached while the atoms drift, with a per-strand bulge of ARC_BULGE plus a seeded offset of at most ARC_JITTER, and an opacity that decreases monotonically with age and reaches zero at STRAND_FADE_MS; call it from frame() immediately before drawTrans so travelling heads sit on top of their own threads
T5: in harness-console/index.html change the directed branch of drawTrans so the travelling head follows the same arc via arcPoint instead of interpolating along the straight chord, draw the head only while the transmission age is below TRAVEL_MS, leave the broadcast expanding-ring branch untouched, and skip the travelling head entirely when the existing reduce flag is set so reduced-motion users get the thread without the orbit
