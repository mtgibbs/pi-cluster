T1: create scripts/ralph-ledger.sh — the sidecar helper, sourced by the loops AND executable as a CLI. macOS bash 3.2 ONLY: no `declare -A`, no `mapfile`/`readarray`, no `${var,,}`, and never expand a possibly-empty array as `"${arr[@]}"` under `set -u` (that is a fatal unbound-variable error on 3.2, and it is the single most repeated mistake in this repo — see AGENTS.md). Use temp files or parallel indexed arrays instead. Define EXACTLY these functions, all of which must `return 0` on every path and print NOTHING on stderr ever: (a) `ledger_dir` prints `${RALPH_LEDGER_DIR:-$HOME/.harness/ledger}`. (b) `ledger_key` prints `<project>__<slug>` where project = `basename` of `git config --get remote.origin.url` with a trailing `.git` stripped, falling back to `basename` of `git rev-parse --show-toplevel` when there is no origin remote, and slug = `$SPEC_DIR` made relative to the repo toplevel with every `/` replaced by `-`; strip any trailing slash from SPEC_DIR first so `specs/foo` and `specs/foo/` give the same key. (c) `ledger_file` prints `$(ledger_dir)/$(ledger_key).tsv`. (d) `ledger_record <task-label> <commit> <attempt> <gate-output>` creates the dir if needed and APPENDS exactly one line, six TAB-separated fields: task-label, commit, epoch seconds, attempt, count-of-passed-checks, and a COMMA-separated list of passed check names. A check name is the second whitespace token of a line whose FIRST token is `PASS` or `ok`; lines whose first token is `FAIL`, `no`, `PEND`, `pend` or `PENDING` are NOT passed, and any other leading token is not a check at all. THE TRAP: a pend line reads `  pend  alpha (not built yet)` and contains the name, so never grep for names — classify by the leading token, exactly as scripts/ralph-retry.sh already does. (e) `ledger_resume_index <tasks-file> <gate-output-file>` prints a single integer: how many LEADING task lines may be skipped. Walk the task file top to bottom; for each line take its label as the text before the first `:`; find that label's LAST row in the ledger; the task is skippable ONLY IF `git merge-base --is-ancestor <row-commit> HEAD` succeeds AND every check name in that row's comma list is currently `PASS`/`ok` in the gate-output file. Stop at the FIRST task that fails either condition and print the count so far — never skip past a gap. If the ledger is missing, unwritable, empty, or any row is malformed, print 0. NOTE: `RALPH_LEDGER_DIR` is an addition to spec §3 for the same reason RALPH_LOG_DIR / RALPH_STATUS_DIR / RETRY_STATE_DIR exist — the gate must never write the operator's real corpus; the DEFAULT is still exactly the spec'd `$HOME/.harness/ledger`.

T2: make scripts/ralph-ledger.sh executable (chmod +x) and give it a CLI dispatcher that runs ONLY when the file is executed, never when it is sourced — guard with `case "${BASH_SOURCE[0]}" in ...` or the equivalent `[ "${BASH_SOURCE[0]}" = "$0" ]` test, because the loops source this file and must not trigger it. Subcommands: no arguments AND `list` both print the current ledger's rows to stdout and exit 0 — when there is no ledger yet they print a line containing the words `no ledger` and still exit 0, never a usage error; `forget <task-label>` removes every row whose first field equals that label and preserves every other row byte-for-byte (write a temp file next to the ledger, then `mv` it into place — never edit in place); `clear` removes the ledger file entirely. An unknown subcommand prints usage to stderr and exits 2. `list` and `forget` on a missing ledger are not errors.

T3: wire the ledger into BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh, identically — RECORDING and RESUMING TOGETHER, in one task. They were split across two tasks and the split is not buildable: the gate's resume checks (ac6, ac8, ac9) are anchored on the ledger being wired AT ALL, not on ledger_resume_index specifically, so wiring recording ARMS them and they fail for work the next task owns. Three consecutive attempts implemented recording correctly and were reset by checks belonging to resuming. Do both halves in this one task. wire RECORDING into BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh, identically. (a) Beside the existing `ralph-status.sh` / `ralph-bus.sh` / `ralph-retry.sh` source blocks, add the same optional-and-never-fatal block for `ralph-ledger.sh`, defining no-op stubs `ledger_record() { :; }` and `ledger_resume_index() { echo 0; }` when the file is absent. (b) In the gate-pass branch — the block starting `if out="$(cd "$ROOT" && bash "$VERIFY" 2>&1)"; then`, at ralph-qwen.sh:137 and ralph-codex.sh:143 — AFTER the `git commit` line (ralph-qwen.sh:140, ralph-codex.sh:146) call `ledger_record "${task%%:*}" "$(git -C "$ROOT" rev-parse HEAD)" "$attempt" "$out"`. Record AFTER the commit, never before, so the recorded sha is the commit that actually contains the work. Do NOT touch the reset, the retry feedback, the no-op guard, or the final STRICT block. ALSO, IN THE SAME TASK: wire RESUMING into BOTH scripts, identically, behind `RALPH_RESUME`. Immediately BEFORE the `while IFS= read -r task` line (ralph-qwen.sh:81, ralph-codex.sh:108), when `"${RALPH_RESUME:-0}" = 1`, run the gate once into a temp file and set `_skip="$(ledger_resume_index "$TASKS" "$tmpfile")"`; otherwise set `_skip=0`. Inside the loop keep a task counter and, while the counter is <= `_skip`, print to stdout one line containing the word `skip`, the task label, and the short sha the decision trusted, then `continue` WITHOUT invoking the executor, WITHOUT committing, and WITHOUT consuming a retry. When RALPH_RESUME is unset or 0 the behaviour must be byte-identical to today — same commits, same message prefix, same exit status. Do NOT touch the final STRICT pass: a resumed run must still be held to the whole spec. NOTHING in this task or T3 may open `$TASKS` for writing — the driver file is read-only to the loop, forever.

NOTE FOR THE OPERATOR (not a task): this spec edits the loop that runs it. Run `scripts/run-loop.sh build-converge specs/tasks-ledger` from an ISOLATED COPY of `scripts/` — bash re-seeks in a running script, so the executing copy must not be the one being edited. Same caution as specs/ralph-retry-contract and specs/run-regression-guard.
