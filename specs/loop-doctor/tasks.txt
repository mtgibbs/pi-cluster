T1: create scripts/loop-doctor.sh per spec.md §2/§7 — bash, `set -uo pipefail`, macOS bash 3.2 (NO declare -A, NO mapfile), executable bit set. Arg parsing only: `--status-dir D` (default "${RALPH_STATUS_DIR:-$HOME/.harness/status}"), `--log-dir D` (default "${RALPH_LOG_DIR:-$HOME/.harness/logs}"), `--ledger F`, `--json`, `--now EPOCH` (default `date +%s`), `--stale-s N` (default 120). An UNKNOWN flag prints a line containing `usage:` to stderr and exits 1. Then apply the two directory rules IN THIS PRECEDENCE, most-specific first — do not reorder them, they overlap: (1) if a directory was EXPLICITLY given via --status-dir or --log-dir and is not a readable directory, print to stderr and exit 2, EVEN IF the other directory is unreadable too, and even if the other is fine; (2) only when NEITHER flag was given, and neither default directory is readable, print a line containing `usage:` to stderr and exit 1. Track "was this flag explicitly given" as a variable set DURING parsing (e.g. SD_GIVEN/LD_GIVEN) — do NOT inspect "$1" after the parse loop, because the loop has shifted every argument away and `set -u` makes that a fatal unbound-variable error. Exit 0 otherwise. Do NOT read any artifacts yet.

T2: extend scripts/loop-doctor.sh — heartbeat discovery + human output. Enumerate files in --status-dir whose basename matches `^[a-z0-9][a-z0-9]*-[0-9][0-9]*\.json$`; run_id = basename minus `.json`. Enumerate subdirectories of --log-dir whose basename matches the same pattern. The set of runs is the UNION of both (spec §6: a run may have a heartbeat with no log dir, or a log dir with no heartbeat). Print EXACTLY one line per run to stdout, in this literal shape, fields separated by TWO spaces: `<run_id>  <fault>  phase=<phase>  <repo>:<spec>  <evidence>`. For this task only, use `fault=unknown`, `evidence=not-yet-classified`, and take phase/repo/spec from the heartbeat JSON via jq (`unknown` for each when the heartbeat is absent or unparseable). Sort by the heartbeat `updated` field DESCENDING, runs with no readable `updated` last, ties broken by run_id ascending. Exit 0.

T3: extend scripts/loop-doctor.sh — `--json` output and the §3.2 heartbeat-sourced fields. When --json is given, print one JSON object per run per line INSTEAD of the human line, carrying every §3.2 field. From the heartbeat: run_id agent repo spec branch task_index total_tasks attempt max_attempts phase verify_pass started updated. Derived: `duration_s` = updated-started, `stale_s` = <--now> - updated. CRITICAL (§7, AC12): `verify_pass` must be emitted as a JSON literal `true`/`false`/`null` and NEVER the string `"null"`; every heartbeat-sourced field is `null` (not 0, not "") when the heartbeat is absent or unparseable, and `stale_s` is `null` when there is no readable `updated`. Build the object with jq so quoting and literals are correct — do not hand-concatenate JSON. Human output from T2 must not change.

T4: extend scripts/loop-doctor.sh — log-directory reader, adding fields `attempts_seen`, `diffs_seen`, `bytes_last`, `unparsed` to the JSON of T3. In `<log-dir>/<run_id>/`: `attempts_seen` = count of files matching `^T[0-9][0-9]*-attempt[0-9][0-9]*\.log$`; `diffs_seen` = count matching the same with `.diff`; `bytes_last` = byte size of the attempt log with the highest (task_index, attempt) pair, or `null` when there are no attempt logs. `unparsed` = count of files in that directory matching NEITHER pattern, PLUS 1 when the run's heartbeat file exists but does not parse as JSON (spec §3.4). When a run has no log directory, attempts_seen=0, diffs_seen=0, bytes_last=null. Append `  (unparsed: N)` to the human line when N>0, and only then.

T5: extend scripts/loop-doctor.sh — the §3.3 classifier, replacing T2's placeholder so `fault` and `evidence` are real in BOTH output modes. Apply the rules in spec.md §3.3 order, first match wins: dead / running / done / watchdog-kill / permission-blocked / executor-stillborn / verify-fail / no-op / unknown. Read at MOST the first 4096 bytes of the last attempt log when matching markers (§8.5). `evidence` must be non-empty for every run (AC7) and must contain ONLY the fixed marker or the named absence — NEVER the surrounding line's payload and never any byte of a `.diff` (§8.4, AC14): for watchdog-kill emit exactly `Killed: 9`, for permission-blocked exactly `permission requested:`, for dead `heartbeat stale <n>s (phase=<p>)`, for running `heartbeat fresh <n>s`, for done `phase=done`, for executor-stillborn `<n>B transcript, no kill marker`, for verify-fail `<basename>.diff present`, for no-op `log <n>B, no .diff`, for unknown `no rule matched: phase=<p> bytes_last=<n> diffs=<n>`. watchdog-kill MUST be tested before executor-stillborn (spec §6 — a 114-byte log containing the kill marker is a watchdog kill, not a stillborn container).

T6: extend scripts/loop-doctor.sh — `--ledger F` append-only JSONL. After computing all rows, append the T3/T4/T5 JSON object for each run to F, one per line, creating F if absent. IDEMPOTENT BY SKIPPING (§8.3): before appending, read the existing F and skip any run whose `run_id` already appears there; NEVER rewrite, truncate, sort, or de-duplicate the existing file. Running twice against the same fixtures must add zero lines the second time (AC13). `--ledger` does not change stdout: human or --json output is still printed. If F's directory does not exist, print to stderr and exit 2 rather than creating parent directories.
