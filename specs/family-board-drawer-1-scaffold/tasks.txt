T1: In clusters/pi-k3s/family-board/index.html, wrap the existing <div class="wrap">…</div> in a new <div class="shell">…</div>; add <aside class="drawer" aria-hidden="true"></aside> inside .shell after .wrap; add <div class="sliver" aria-hidden="true"></div> near .idle-hint; add three CSS rules: `.shell { display: flex; flex-direction: row; overflow-x: hidden; }`, `.drawer { width: 360px; min-height: 100vh; background: linear-gradient(180deg, var(--surface), var(--bg-2)); }`, `.sliver { position: fixed; top: 0; bottom: 0; right: 0; width: 4px; background: var(--gold); opacity: .22; z-index: 4; pointer-events: none; }`. NO JS in this sub-spec — no event listeners, no state vars, no constants. Existing acks/menu/lanes/art-mode/feed remain intact.
