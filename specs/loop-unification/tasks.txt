T1: extract ONE driver and convert BOTH adapters, in this single task — it is deliberately not split, because the gate cannot distinguish a half-done extraction from a broken one and a task whose only failable checks belong to the next task is unbuildable (four consecutive attempts at specs/tasks-ledger T3 proved that this week). (a) CREATE scripts/ralph-loop.sh by MOVING the body of scripts/ralph-qwen.sh into it verbatim — the task queue, the retry contract, the stillborn-executor abort, the no-op guard, the verify.sh invocation, `git add -A` + commit, the STRICT backstop. Do not rewrite any of it; a behaviour change smuggled into a refactor is a defect. Make exactly three edits while moving: replace the single `OC_SHEET=off OC_RUN_TIMEOUT=... oc run --dir "$ROOT" "$prompt" > "$(log_path ...)" 2>&1; _rc=$?` call site with `executor_run "$prompt" "$(log_path "$HB_TASK" "$attempt")"; _rc=$?`; replace the literal `ralph(qwen):` in the commit subject with `ralph(${RALPH_AGENT}):`; and call `executor_preflight` once before the queue starts. ralph-loop.sh must contain NO executor identifier at all — a grep for qwen, codex, `oc run` or opencode in that file is a defect and is the one property the gate can prove by absence. It is SOURCED, not executed, so the adapter's functions are already defined when it runs; guard with a loud failure if `executor_run` is not a defined function. (b) CONVERT scripts/ralph-qwen.sh to an adapter under 30 code lines: set RALPH_AGENT=qwen and RALPH_SHEET default on, define executor_preflight as a no-op (oc needs none today), define executor_run to run `OC_SHEET=off OC_RUN_TIMEOUT="${OC_RUN_TIMEOUT:-480}" oc run --dir "$ROOT" "$1" > "$2" 2>&1`, then source ralph-loop.sh. (c) CONVERT scripts/ralph-codex.sh identically: RALPH_AGENT=codex, RALPH_SHEET default off, executor_preflight carrying the existing `command -v codex` and `codex login status` checks verbatim, and executor_run carrying the existing run_codex body verbatim INCLUDING its own watchdog — codex has no OC_RUN_TIMEOUT equivalent and the driver must not impose one or the two watchdogs will fight. Neither adapter may retain a task-queue read, a retry counter, a verify.sh invocation or a git commit. Both entry points keep their exact current CLI and behaviour. Run `bash -n` on all three files before you finish.
