T1: in scripts/harness inside the run) case, parse an optional --repo <name> flag out of the positional arguments before target and spec are assigned, so that flag order does not matter and the remaining positionals keep their current meaning (target, spec, base-branch); store the value in a variable named repo defaulting to the empty string; do not change any behaviour yet
T2: in scripts/harness validate the repo value immediately after parsing — if repo is non-empty and does not match the regex ^[A-Za-z0-9._-]+$ then print an error naming the offending value and telling the operator a bare repository name is expected (not a URL or path) and exit 1 before any ssh call is made; this value is interpolated into a remote command so it must be rejected, never escaped
T3: in scripts/harness use the repo value when building task_cmd — when repo is empty keep both existing branches of the if exactly as they are today (pi-cluster for claude claude2 codex, no repo token for qwen) so the default is byte-identical; when repo is non-empty APPEND the two tokens --repo and the repo name to the end of the existing task_cmd for whichever branch applies, shifting no positional and collapsing nothing; and extend the final echo line to name the repo the run targets
