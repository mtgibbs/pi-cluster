T1: create scripts/ralph-retry.sh — the SOURCED best-effort helper (never executed directly), mirroring the shape of scripts/ralph-log.sh: a `# shellcheck shell=bash` header, a comment block saying why it exists, and functions that can never fail their caller. macOS bash 3.2, no `declare -A`, no `mapfile`. Define exactly three functions. `retry_init` — start a new task: set RETRY_OK=1 and create a fresh empty state file at "${RETRY_STATE_DIR:-${TMPDIR:-/tmp}}/ralph-retry-$$.passed"; if it cannot be created, set RETRY_OK=0 and return 0 (never nonzero). `retry_record <gate-output>` — append to the state file the check NAME of every line in the gate output classified as passed; classification is by LEADING TOKEN ONLY, case-insensitively, using the same awk rule as scripts/gate-score.sh lines 45-59 (1-4 leading spaces then a word; PASS or OK -> passed; FAIL -> failed; PEND or PENDING -> pending; any other leading token is not a check and is ignored). The check NAME is the first whitespace-delimited field AFTER the verdict token. NEVER substring-match a verdict word anywhere in a line — a PASS line whose message contains the word "fail" is still a PASS. `retry_regressions <gate-output>` — print, one per line, at most 10, the names that appear in the state file AND are classified failed or pending in this gate output; print nothing when the state file is missing, RETRY_OK=0, or there is no overlap. All three return 0 always.

T2: fix the reset in BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh — add `git -C "$ROOT" reset -q -- .` as a NEW line IMMEDIATELY BEFORE the existing `git -C "$ROOT" checkout -- . 2>/dev/null || true` line, with a trailing comment explaining that checkout restores from the INDEX so a staged file survives it. Keep the two existing lines exactly as they are. Do NOT use `reset --hard`, do NOT add `-x` to the clean, do NOT touch any other git command: the gitignored opencode.json MUST survive a reset or the next attempt is auto-rejected on every tool call. The two scripts are deliberate structural twins — the added line and its comment must be IDENTICAL in both files.

T3: fix the prompt in BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh — replace the single line `Follow the spec's section 10 reference and section 7 acceptance criteria EXACTLY.` with two lines: `Follow the spec's section 10 acceptance criteria and section 7 norms EXACTLY.` and `Do not run git add, git commit, or git stash — the loop owns the index.` (the current line has the section numbers backwards: in specs/TEMPLATE.md §7 is Norms and §10 is Acceptance criteria). Change nothing else in the prompt string, and make the replacement IDENTICAL in both files.

T4: wire the helper into BOTH scripts/ralph-qwen.sh AND scripts/ralph-codex.sh. (a) Source it beside the existing ralph-log.sh source block, with no-op stubs when the file is absent, exactly matching the existing pattern — `retry_init() { :; }; retry_record() { :; }; retry_regressions() { :; }`. (b) Call `retry_init` once per TASK, next to where the per-task loop resets `feedback=""` and `passed=0`. (c) Call `retry_record "$out"` immediately after the gate runs, on BOTH the pass and the fail path, so a check that passed in a passing attempt is still remembered. (d) On the verify-FAILURE path ONLY — the `feedback=` assignment that says "A previous attempt FAILED verification with:", NOT the earlier no-op-guard `feedback=` that says "produced NO file changes at all" — append a regression block to `feedback` when `retry_regressions "$out"` prints anything, in this literal shape: a line `REGRESSION — these checks PASSED in an earlier attempt of this same task and now do not:`, then the names, then a line `Keep them passing while you fix the failures above. Do not trade one check for another.` When there are no regressions, `feedback` must be byte-identical to what it is today.
