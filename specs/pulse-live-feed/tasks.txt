T1: in harness-console/index.html add the feed-state machine — a single variable feedState holding exactly one of the strings 'live' 'stale' 'sim', constants STREAM_STALE_MS=45000 TX_MIN_GAP_MS=260 TX_QUEUE_MAX=24 RECYCLE_MIN_MS=3000 RECYCLE_MAX_MS=30000, and trackers lastStateOk lastStreamMsg pollFails txQueue reconnectDelay; add a function setFeedState(next) that assigns feedState, toggles the existing 'live' class on document.body and controlsEl only when next==='live', and updates the feed pill text to 'LIVE FEED' for live, 'RECONNECTING' for stale, 'SIMULATED' for sim; do not remove liveMode yet
T2: rewrite poll() in harness-console/index.html to use hysteresis — on success set lastStateOk=Date.now(), reset pollFails to 0 and call setFeedState('live') immediately; on failure increment pollFails and only call setFeedState('sim') once pollFails is 3 or more, leaving the display unchanged below that threshold; keep applyLive(d) on success and keep liveMode in sync with feedState==='live'
T3: in harness-console/index.html wrap EventSource creation in a function openStream() that closes any previous EventSource first and assigns the new one to a single module-scope variable, sets lastStreamMsg=Date.now() in onopen and in onmessage, and resets reconnectDelay to RECYCLE_MIN_MS in onopen; keep the existing 30-second backfill age guard inside onmessage unchanged; call openStream() once at startup
T4: in harness-console/index.html add a watchdog on setInterval every 5000 ms that calls openStream() and then sets reconnectDelay to Math.min(reconnectDelay*2, RECYCLE_MAX_MS) only when feedState is 'live' and Date.now()-lastStreamMsg is greater than STREAM_STALE_MS and Date.now()-lastStreamMsg is greater than reconnectDelay — a wedged stream is the case where the state poll is healthy but no SSE frame has arrived
T5: in harness-console/index.html add document.addEventListener('visibilitychange', ...) that when document.visibilityState==='visible' immediately calls poll() and, if Date.now()-lastStreamMsg is greater than STREAM_STALE_MS, calls openStream() — background tabs have their timers throttled so the page must revalidate on return rather than wait
T6: in harness-console/index.html route accepted events through txQueue instead of calling emit() directly from onmessage — push {from,targets} onto txQueue, discard from the front while txQueue.length exceeds TX_QUEUE_MAX, and add a pump on setInterval TX_MIN_GAP_MS that shifts at most one entry per tick and passes it to emit(); leave stepSimTrans and the emit() signature unchanged
