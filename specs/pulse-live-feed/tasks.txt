T1: in harness-console/index.html add the constants STREAM_STALE_MS=45000 TX_MIN_GAP_MS=260 TX_QUEUE_MAX=24 RECYCLE_MIN_MS=3000 RECYCLE_MAX_MS=30000, the trackers feedState lastStateOk lastStreamMsg pollFails txQueue reconnectDelay, and the two PURE functions feedNext(state, pollOk, pollFails, msSinceStream) and txAdmit(queue, ev, max) copied EXACTLY from the WORKED EXAMPLE in section 6 of specs/pulse-live-feed/spec.md — they must read no globals except the constants, touch no DOM and no timers; also add setFeedState(next) which assigns feedState, toggles the existing live class on document.body and controlsEl only when next is live, and sets the feed pill text to LIVE FEED for live, RECONNECTING for stale and SIMULATED for sim
T2: in harness-console/index.html rewrite poll() to route every decision through feedNext — on success set lastStateOk to Date.now() and pollFails to 0, on failure increment pollFails, then in both cases call feedNext with the current feedState and the observations and pass its returned state to setFeedState; keep applyLive(d) on success and keep liveMode in sync with feedState being live; do not implement recycling yet
T3: in harness-console/index.html wrap EventSource creation in a function openStream() that closes any previous EventSource first and assigns the new one to a single module-scope variable, sets lastStreamMsg=Date.now() in onopen and in onmessage, and resets reconnectDelay to RECYCLE_MIN_MS in onopen; keep the existing 30-second backfill age guard inside onmessage unchanged; call openStream() once at startup
T4: in harness-console/index.html add a watchdog on setInterval every 5000 ms that calls feedNext with the current feedState, the last poll outcome, pollFails and Date.now()-lastStreamMsg, and when the returned recycle is true calls openStream() and then sets reconnectDelay to Math.min(reconnectDelay*2, RECYCLE_MAX_MS) — do not re-derive the recycle condition inline, the pure function is the only place that decision is made
T5: in harness-console/index.html add document.addEventListener('visibilitychange', ...) that when document.visibilityState==='visible' immediately calls poll() and, if Date.now()-lastStreamMsg is greater than STREAM_STALE_MS, calls openStream() — background tabs have their timers throttled so the page must revalidate on return rather than wait
T6: in harness-console/index.html route accepted events through txQueue using txAdmit(txQueue, ev, TX_QUEUE_MAX) instead of calling emit() directly from onmessage, and add a pump on setInterval TX_MIN_GAP_MS that shifts at most one entry per tick and passes it to emit(); leave stepSimTrans and the emit() signature unchanged
