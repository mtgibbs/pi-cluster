# Run Index — every task the loop ran, and how it went

Generated by `scripts/run-index.py` on 2026-08-24 21:29 EDT from `feat/evidence-convention` @ `60547f6`.
Re-run it after every task; it derives everything and stores nothing.

A `—` means **the data does not exist**, not zero. Columns that are mostly dashes
are the honest output of stores that were added partway through the run.

## Totals

- **3 tasks** known to any store — 3 landed a commit under their own task id, 0 did not
- **4 task commits** (some tasks were requeued and committed twice)
- **1 executor runs** attributed to this project
- **0 runs** in the evidence bundle belong to a *different* project
- **0 runs** cannot be attributed to any task
- **— judge findings** in the ledger — *ledger not reachable from this checkout (no ledger.jsonl under 1 candidate location(s) below /Users/mtgibbs/dev/pi-cluster/.git)*

## Per task

| Task | Commit | When | Diff | Attempts | Failed | Gate after | Cum. gaps | Runs | Follow-ups |
|---|---|---|---|---|---|---|---|---|---|
| **T1 —** ⚠︎ | `60547f6` | 08-24 21:29 | 2f +42/-0 | 1 | — | — | — | 1 | — |
| **T2 —** | `8b31c08` | 08-24 20:21 | 2f +2/-2 | — | — | — | — | — | — |
| **T3 —** | `40b0137` | 08-24 20:24 | 2f +2/-0 | — | — | — | — | — | 1 |

⚠︎ = committed more than once (reverted and requeued). ✗ = **queued but never landed a commit under its own task id**.

Where Attempts/Failed reads `a / **b**`, the two stores disagree: `a` is what `metrics.jsonl` recorded (the winning run only), **`b`** is how many attempt logs actually survive across every run of that task. The gap is abandoned runs — work that happened and was never counted.

## Task detail

### T1 —

- **commit** `0bfe244` — 2026-08-24 20:19 — ralph(qwen): T1 — in scripts/ralph-status.sh make the status sweep configurable — replace the hardcoded `-mmin +1440` at
- **commit** `60547f6` — 2026-08-24 21:29 — ralph(qwen): T1 — in scripts/ralph-status.sh make the status sweep configurable — replace the hardcoded `-mmin +1440` at
- **executor runs**
    - `qwen-19281` · 08-24 21:27 · 1m · 1 attempt logs / 0 failed-diffs · phase `passed` · verify PASS · task id via status · log only

### T2 —

- **commit** `8b31c08` — 2026-08-24 20:21 — ralph(qwen): T2 — in scripts/ralph-log.sh and scripts/ralph-status.sh default the roots to the TARGET REPO's .evidence/ 
- **executor runs** — *no run in either log store maps to this task*

### T3 —

- **commit** `40b0137` — 2026-08-24 20:24 — ralph(qwen): T3 — in scripts/ralph-qwen.sh and scripts/ralph-codex.sh run scripts/loop-index.py against the target repo 
- **executor runs** — *no run in either log store maps to this task*
- **follow-up commits** (what this task's output forced afterwards)
    - `dc38666` *fix* — fix: one task-label grammar, scope the index by spec dir, and two rules the run taught us

## Judge findings

**Ledger not reachable from this checkout** — no ledger.jsonl under 1 candidate location(s) below /Users/mtgibbs/dev/pi-cluster/.git.

This is *unavailable*, not *empty*. `ralph-judge.sh` writes its state to `git rev-parse --git-path ralph-judge`, which resolves to `.git/worktrees/<name>/ralph-judge` in a linked worktree and `.git/ralph-judge` in the main checkout — so a judge that ran in a worktree is invisible from the other side. Re-run this script from the checkout the judge ran in, or pass `--repo` pointing at it.

## What these stores cannot answer

**The judge ledger could not be read from this checkout**, so nothing here describes it. See the Judge findings section above — and do not read its absence as an absence of findings.

